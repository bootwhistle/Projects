// setupTests.js
//
// This file runs automatically before every test file (configured in
// vite.config.js → test.setupFiles).
//
// It imports jest-dom which adds extra matchers to Vitest's `expect`:
//   toBeInTheDocument()   — element exists in the DOM
//   toHaveValue()         — input has a specific value
//   toBeDisabled()        — button/input is disabled
//   toHaveTextContent()   — element contains specific text
//   ...and many more (see: https://github.com/testing-library/jest-dom)

import '@testing-library/jest-dom';
